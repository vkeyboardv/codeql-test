const { checkOne } = require('./hello.js')

console.log('hello world')

checkOne()

const dom, contactDetails, phonesTemplate, emailsTemplate, dom;

if (contactDetails === NaN) {
  console.log('omg')
}

function createObjectWrite() {
  const assignment = `obj[${JSON.stringify(key)}]=42`;
  return `(function(){${assignment}})` // NOT OK
}
